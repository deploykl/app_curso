# Entorno de pruebas: nunca SMTP real.
# "json" hace que nodemailer serialice el correo en vez de enviarlo, así la
# suite no depende de que MailHog esté levantado ni toca la cuenta de Gmail.
SMTP_HOST="json"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASSWORD=""
